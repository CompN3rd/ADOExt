Run TypeScript type checking only (no esbuild bundle) — faster than a full compile.

Execute: `npm run check:extension && npm run check:webviews`

Report all type errors with file:line references. If clean, confirm both host and webview type checks passed.
